# FastAPI Application

This is a simple FastAPI application.

## Running the Application

To run the application, use the following command:

```bash
uvicorn src.main:app --reload
```

## Running Tests

To run the tests, ensure you have pytest installed and use:

```bash
pytest
```
